extends Node2D


func _ready() -> void:
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta: float) -> void:
	pass#if score <= 20:
		#var mob = wall_scene.instantiate()
		#add_child(mob)
		#score += 1

#func ran():
	
